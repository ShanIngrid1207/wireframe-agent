{
  "name": "wireframe-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Wireframe Agent — CSV of Google Form responses → Claude → wireframe specs. Backend serves the built frontend as a single app.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/index.js"
  }
}
